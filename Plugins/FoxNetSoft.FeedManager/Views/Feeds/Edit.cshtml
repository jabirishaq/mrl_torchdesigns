@model FoxNetSoft.Plugin.Feed.Manager.Models.FeedRecModel
@using FoxNetSoft.Plugin.Feed.Manager.Models
@using Nop.Web.Framework
@{
    Layout = "~/Administration/Views/Shared/_AdminLayout.cshtml";
    //page title
    ViewBag.Title = @T("Admin.FoxNetSoft.Plugin.Feed.Manager.Feed.Edit").Text;
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "feed-form" }))
{
    <div class="section-header">
        <div class="title">
            <img src="@Url.Content("~/Administration/Content/images/ico-catalog.png")" alt="" />
            @T("Admin.FoxNetSoft.Plugin.Feed.Manager.Feed.Edit") - @Model.Name @Html.ActionLink("(" + T("Admin.FoxNetSoft.Plugin.Feed.Manager.Feed.BackToList") + ")", "List")
        </div>
        <div class="options">
            @if (!Model.IsRunning)
            {
                <a href="@Url.Action("Generate", new { Id = Model.Id})" class="k-button">@T("Admin.FoxNetSoft.Plugin.Feed.Manager.Feed.Generate")</a>
            }
            <button type="submit" name="save" value="save" class="k-button">@T("Admin.Common.Save")</button>
            <button type="submit" name="save-continue" value="save-continue" class="k-button">@T("Admin.Common.SaveContinue")</button>
            <span id="feed-delete" class="k-button">@T("Admin.Common.Delete")</span>
        </div>
    </div>
    @Html.Partial(String.Format(Model.ViewPath,"_CreateOrUpdate"), Model) 
}
@Html.DeleteConfirmation("feed-delete")
