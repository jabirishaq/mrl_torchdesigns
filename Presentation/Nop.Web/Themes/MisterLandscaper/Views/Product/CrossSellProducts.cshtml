@model IList<ProductOverviewModel>
@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    @*<div class="product-grid cross-sells">
        <div class="title">
            <strong>@T("ShoppingCart.CrossSells")</strong>
        </div>
        @foreach (var product in Model)
        {
            <div class="item-box">
                @Html.Partial("_ProductBox", product)
            </div>
        }
    </div>*@

    <div class="crosssalescorouselslidermaindiv">
        <script>
            //$(document).ready(function () {
            $('.crosssalescorousel').jcarousel({
                list: '.crosssalesproductthome-page-category-grid',
                items: '.relatedproductitem-box'
            });

            //});
        </script>
        <div class="title">
            <strong>@T("ShoppingCart.CrossSells")</strong>
        </div>
        <div class="crosssalescorousel">

            <div class="crosssalesproductthome-page-category-grid">


                @foreach (var product in Model)
                {  <div class="crosssalesproductitem-box">
                    @Html.Partial("_ProductBox", product)
                </div>
                    
                }
           
            </div>
        </div>
        <a href="#" class="crosssalescorouseljcarousel-control-prev"></a>
        <a href="#" class="crosssalescorouseljcarousel-control-next"></a>
    </div>
}

